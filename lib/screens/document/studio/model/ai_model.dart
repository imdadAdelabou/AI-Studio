/// Contains the AI model class
class AiModel {
  /// Contains the title of the AI model
  const AiModel({
    required this.title,
    required this.key,
  });

  /// Contains the title of the AI model
  final String title;

  /// Contains the key of the AI model
  final String key;
}
